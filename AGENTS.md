# woooosh - Agent Notes

## Version Bumping
- Always bump `package.json` version when landing a feature branch to `main`
- The version is displayed in the app footer (`#appVersion`)
- Format: semantic versioning (major.minor.patch)
